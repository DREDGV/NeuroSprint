# NeuroSprint Execution Status (RU)

## Дата/время
- 2026-02-24 19:13

## Что завершено
- MVP (M0-M6) реализован и стабилизирован.
- Выполнен переход на новую IA: `Главная | Тренировки | Статистика | Профили | Настройки`.
- Добавлены маршруты `training`/`stats individual/group` и редиректы со старых путей.
- Реализованы режимы `Classic+`, `Timed+`, `Reverse` с единым стартом сессии.
- Добавлены пресеты и расширенные параметры режима.
- Добавлена адаптивная сложность (оценка по окну из 3 сессий) + ручной override.
- Расширена статистика пользователя: тренды, week-over-week, рекомендации.
- Обновлена Dexie схема до v3, добавлены таблицы для групповой аналитики.
- Обновлены и расширены тесты unit/integration/e2e.
- Улучшен экран `/stats/group`: фильтры периода 7/14/30/90/all, метрики лучший/средний/худший, перцентиль, гистограмма распределения уровней.
- Исправлен расчет перцентиля по группе: теперь учитывается выбранный `modeId`.
- Добавлены unit-тесты на распределение уровней.
- В шапку приложения добавлено отображение версии (`v0.2.0`) и усилен бренд-блок (крупнее заголовок + mark).
- Добавлен сравнительный блок в индивидуальной статистике: `Я / другой пользователь / моя группа / все пользователи` с фильтрами метрики и периода.
- Добавлен сравнительный блок в групповой статистике: текущая группа vs другая группа vs все пользователи.
- Добавлен unit-тест `mode-metric-snapshot` для общей сводки сравнения.
- Добавлен fixture-генератор в `Настройки`: демо-класс 30+ (2 группы, 30 учеников, 14 дней сессий).
- Добавлены integration-тесты для сравнительных блоков статистики.
- Добавлен e2e-сценарий генерации fixture и проверки `individual/group` сравнений.
- Полный регресс подтвержден: `npm test`, `npm run build`, `npm run test:e2e`.
- Оптимизированы агрегации для групповой аналитики: выборки сессий по compound-index `[userId+moduleId+modeId]` и фильтрация по `localDate`.
- После оптимизации подтверждены `npm test` и `npm run build`.
- Зафиксирован релиз `alpha-v0.3.0`: версия проекта обновлена до `0.3.0`, добавлен документ `docs/ALPHA_v0.3.0_RELEASE_NOTES_RU.md`.
- Полный регресс для релиз-кандидата подтвержден: `npm test`, `npm run build`, `npm run test:e2e`.
- UX-hardening: генератор fixture в `Настройки` стал настраиваемым (число групп, учеников, дней) + подтверждение перед генерацией.
- Улучшена читаемость на мобильных экранах и поведение длинных списков участников в групповой статистике.
- После hardening повторно подтвержден полный регресс: `npm test`, `npm run build`, `npm run test:e2e`.
- В `Настройки` добавлен инструмент замера агрегаций (`30/90/all`) для контроля производительности на больших локальных наборах.
- После добавления замеров повторно подтвержден полный регресс: `npm test`, `npm run build`, `npm run test:e2e`.
- Завершена оптимизация периода агрегаций: добавлены индексы Dexie `[modeId+localDate]` и `[userId+moduleId+modeId+localDate]`, выборки статистики переведены на range-query по `localDate`.
- Benchmark-отчет в `Настройки` дополнен порогами и статусом `OK/ВНИМАНИЕ` по периодам `30/90/all`.
- Добавлен e2e smoke `tests/e2e/benchmark.spec.ts` (fixture -> benchmark -> проверка отчета).
- После оптимизаций повторно подтвержден полный регресс: `npm test`, `npm run build`, `npm run test:e2e` (4 e2e теста).

## Что в работе
- v0.4 planning: выбор следующего тренировочного модуля и границ итерации.

## Блокеры
- Нет технических блокеров на текущий момент.

## Следующие 3 шага
1. Подготовить roadmap `v0.4` с первым новым модулем и DoD.
2. Зафиксировать контракты нового режима (`типы`, `scoring`, `session` поля).
3. Реализовать `v0.4.A` как вертикальный срез (экран тренировки + сохранение + базовая статистика).

## Команды для быстрого старта
```powershell
npm install
npm run dev
npm run build
npm test
npm run test:e2e
```
